name: Quick Test Build

on:
  workflow_dispatch:
  push:
    branches: ["**"]
    paths-ignore:
      - "README.md"
      - "fastlane/**"
      - "assets/**"
      - ".github/**/*.md"
      - ".github/FUNDING.yml"
      - ".github/ISSUE_TEMPLATE/**"
      - "app/build.gradle.kts"
      - "changelog.md"

permissions:
  contents: write

concurrency:
  group: quick-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build_quick:
    name: Quick Universal Release Build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6
        with:
          submodules: recursive

      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          java-version: "21"
          distribution: "temurin"

      - name: Set Up Gradle
        uses: gradle/actions/setup-gradle@v5
        with:
          cache-encryption-key: ${{ secrets.GRADLE_CACHE_KEY }}
          cache-cleanup: on-success

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Restore release keystore
        run: |
          mkdir -p app/keystore
          echo "${{ secrets.KEYSTORE }}" | base64 --decode > app/keystore/release.keystore

      - name: Build Release APK (No Lint)
        shell: bash
        run: |
          set +e
          ./gradlew --console=plain assembleFossRelease --warning-mode summary -x lint -x lintFossRelease > gradle-build.log 2>&1
          STATUS=$?
          if [ "$STATUS" -ne 0 ]; then
            echo "Gradle failed; showing the final 500 log lines"
            tail -n 500 gradle-build.log
          else
            tail -n 100 gradle-build.log
          fi
          exit "$STATUS"
        env:
          METROLIST_APPLICATION_ID: com.metrofuse.plus.test
          METROLIST_APP_NAME: MetroFuse+ Testing
          RELEASE_KEYSTORE_PATH: keystore/release.keystore
          STORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
          PULL_REQUEST: "false"
          GITHUB_EVENT_NAME: ${{ github.event_name }}
          LASTFM_API_KEY: ${{ secrets.LASTFM_API_KEY }}
          LASTFM_SECRET: ${{ secrets.LASTFM_SECRET }}

      - name: Move signed APK
        run: |
          OUTPUT_DIR="app/build/outputs/apk/foss/release"
          mkdir -p "$OUTPUT_DIR/out"
          SIGNED_APK=$(find "$OUTPUT_DIR" -maxdepth 1 -type f -name "*universal*release.apk" | head -1)
          if [ -z "$SIGNED_APK" ]; then
            SIGNED_APK=$(find "$OUTPUT_DIR" -maxdepth 1 -type f -name "*release.apk" | head -1)
          fi
          test -n "$SIGNED_APK"
          mv "$SIGNED_APK" "$OUTPUT_DIR/out/MetroFusePlus-test-release.apk"

      - name: Upload Signed APK
        uses: actions/upload-artifact@v6
        with:
          name: MetroFusePlus-test-release
          path: app/build/outputs/apk/foss/release/out/*
